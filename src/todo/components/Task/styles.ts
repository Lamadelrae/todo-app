import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    task: {
        borderRadius: 4, 
        flexDirection: 'row',
        backgroundColor: "#6d6d6d",
        marginHorizontal: 50,
        padding: 10,
    }
});
